---
import ProjectImage from './ProjectImage.astro';

interface Props {
	slug: string;
	title: string;
	tagline: string;
	category: string;
	status: 'Complete' | 'Upcoming';
	heroImage?: string;
	cardBanner?: string;
}

const { slug, title, tagline, category, status, heroImage, cardBanner } = Astro.props;
---

<a class="card" href={`/projects/${slug}/`}>
	<ProjectImage src={heroImage} alt={title} ratio="4 / 3" />
	<div class="card-body">
		<div class="card-meta">
			<span class="tag">{category}</span>
			{status === 'Upcoming' && <span class="tag status-upcoming">Upcoming</span>}
		</div>
		<h3>{title}</h3>
		<p>{tagline}</p>
		{cardBanner && <span class="card-banner">{cardBanner}</span>}
	</div>
</a>

<style>
	.card {
		display: flex;
		flex-direction: column;
		border: 1px solid var(--border);
		border-radius: 14px;
		background: var(--bg-raised);
		overflow: hidden;
		transition:
			transform 0.15s ease,
			border-color 0.15s ease;
	}
	.card:hover {
		transform: translateY(-3px);
		border-color: var(--accent-dark);
	}
	.card :global(.img-slot) {
		border-radius: 0;
		border: none;
		border-bottom: 1px solid var(--border);
	}
	.card-body {
		padding: 1.1em 1.2em 1.3em 1.2em;
		display: flex;
		flex-direction: column;
		flex: 1;
	}
	.card-meta {
		display: flex;
		gap: 0.5em;
		margin-bottom: 0.7em;
	}
	.card h3 {
		font-size: 1.15em;
		margin-bottom: 0.35em;
	}
	.card p {
		margin: 0 0 0.7em 0;
		font-size: 0.92em;
		color: var(--muted);
	}
	.card-banner {
		display: block;
		margin-top: auto;
		padding: 0.6em 1em;
		border-radius: 999px;
		border: 1px solid var(--accent-dark);
		background: rgba(90, 160, 220, 0.1);
		color: var(--accent);
		font-size: 0.85em;
		font-weight: 700;
		text-align: center;
	}
</style>